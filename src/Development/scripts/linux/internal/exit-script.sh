#!/bin/bash
back_value="$1"
msg_value="$2"
# go back
cd $back_value
echo $msg_value
echo "=============================================================================================================================="